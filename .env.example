# Legacy Better Auth settings (not used by SparkX login flow)
BETTER_AUTH_SECRET=replace-with-a-strong-random-secret
# Optional:
# - Can be full URL (https://app.example.com)
# - Can be host-only (app.example.com), protocol auto-added
# - If omitted, auth infers protocol/host from request headers (works on Vercel/proxy)
BETTER_AUTH_URL=http://localhost:3000
DATABASE_URL=mysql://USER:PASSWORD@127.0.0.1:3306/DB_NAME
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# SparkX login API
SPARKX_API_BASE_URL=http://47.112.97.49:6001
# Optional (falls back to BETTER_AUTH_SECRET if omitted)
SPARKX_SESSION_SECRET=replace-with-a-strong-random-secret
# Optional session TTL in seconds (default 2592000 = 30 days)
SPARKX_SESSION_MAX_AGE=2592000
